object Testing {
    private const val junitVersion = "4.13.2"
    const val junit4 = "junit:junit:$junitVersion"

    private const val junitAndroidExtVersion = "1.1.5"
    const val junitAndroidExt = "androidx.test.ext:junit:$junitAndroidExtVersion"

    const val kotlin = "org.jetbrains.kotlin:kotlin-test:${Kotlin.kotlinVersion}"
    const val kotlinJunit = "org.jetbrains.kotlin:kotlin-test-junit:${Kotlin.kotlinVersion}"

    private const val truthVersion = "1.1.3"
    const val truth = "com.google.truth:truth:$truthVersion"

    private const val mockkVersion = "1.12.4"
    const val mockk = "io.mockk:mockk:$mockkVersion"
    const val mockkAndroid = "io.mockk:mockk-android:$mockkVersion"

    private const val turbineVersion = "0.12.3"
    const val turbine = "app.cash.turbine:turbine:$turbineVersion"

    private const val mockWebServerVersion = "4.9.3"
    const val mockWebServer = "com.squareup.okhttp3:mockwebserver:$mockWebServerVersion"
    const val composeVersion = "1.3.3"
    const val composeUiTest = "androidx.compose.ui:ui-test-junit4:$composeVersion"

    private const val navigationTestVersion = "2.5.3"
    const val navigationTest = "androidx.navigation:navigation-testing:$navigationTestVersion"

    private const val testRunnerVersion = "1.4.0"
    const val testRunner = "androidx.test:runner:$testRunnerVersion"

    private const val robolectricVersion = "4.10"
    const val robolectric = "org.robolectric:robolectric:$robolectricVersion"

    private const val androidxTestversion = "1.5.0"
    const val androidTestCoreKtx = "androidx.test:core-ktx:$androidxTestversion"

    private const val espressoVersion = "3.5.1"
    const val espresso = "androidx.test.espresso:espresso-core:$espressoVersion"
    const val espressoContrib = "androidx.test.espresso:espresso-contrib:$espressoVersion"

    private const val jUnitExtVersion= "1.1.5"
    const val testExtension     = "androidx.test.ext:junit:$jUnitExtVersion"
    const val testExtensionKtx  = "androidx.test.ext:junit-ktx:$jUnitExtVersion"

    private const val androidxArchTestVersion = "2.1.0"
    const val androidArchTest   = "androidx.arch.core:core-testing:$androidxArchTestVersion"
}
